void main() {
  var name = '드미'; //var에는 초기값으로 아무거나 넣을수있음
  //name = 1; //그러나 초기값 으로 넣은 변수형과 일치시켜서 사용해야함
  //name = false; //초기값과 변수형이 다르면 오류뜸
  name = '데미안';

  String youName = 'you'; //명시적으로 타입을 지정할수도 있음
  youName = '너';
  print(name);
  print(youName);
}
